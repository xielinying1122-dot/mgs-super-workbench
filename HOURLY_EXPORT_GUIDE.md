# 聚光分时报表 · 导出指引

> 时报需要**分时维度**的数据，跟日报数据是两份。
> 这份文档告诉你怎么从聚光后台导出分时数据，然后一行命令出今天 8-10 点的时报+可视化。

---

## 一、操作步骤（聚光后台）

### Step 1：登录聚光

```
https://ad.xiaohongshu.com/aurora/ad/manage/campaign?vSellerId=692556d24b69aa0015fefdd3
```

### Step 2：进入数据报表

左侧菜单 → **数据** → **数据报表**（或 **数据中心**）

### Step 3：选择"分时段"维度

在报表配置里：
- **时间粒度**：选「**小时**」或「**时段**」（不要选"日"）
- **统计维度**：勾选「时间」「计划名称」「创意名称」（最少这三个）
- **统计指标**：勾选「消费」「展现量」「点击量」「互动量」「点击率」
- **日期范围**：选「**今天**」
- **时段过滤**（如果有）：可以全选 0-23，下载后脚本会按你要的范围筛

### Step 4：导出

点「导出」或「下载报表」按钮，浏览器会下载一份 CSV/XLSX。
**文件名建议改成含「分时」或「时段」**，例如：

```
重命名为：聚光分时_蔡司_20260521.csv
放到：~/juguang-automation/data/raw/
```

> 脚本会自动识别 `*分时*.csv`、`*时段*.csv`、`*hourly*.csv` 等模式

### Step 5：跑时报

```bash
cd ~/juguang-automation
python3 juguang_hourly.py --latest --hours 8-10 --project 蔡司眼镜
```

输出：
- 终端打印纯文本时报
- `~/juguang-automation/data/reports/时报_蔡司眼镜_20260521_08-10.txt`
- `~/juguang-automation/data/reports/时报_蔡司眼镜_20260521_08-10.png`（自动打开）

---

## 二、数据列要求

脚本能自动处理两种聚光分时报表的列结构：

### 模式 A：时间列含小时
```
时间                  | 计划名称 | 消费 | 展现量 | ...
2026-05-21 08:00:00 | XXX    | ...
2026-05-21 09:00:00 | YYY    | ...
```

### 模式 B：时间 + 时段两列
```
时间        | 时段      | 计划名称 | 消费 | ...
2026-05-21 | 08:00     | XXX    | ...
2026-05-21 | 09:00     | YYY    | ...
```

只要列名匹配（时间/日期、时段/小时、计划名称、消费、展现量、点击量），脚本就能跑。

---

## 三、常见情况

### 情况 1：聚光不支持「分时」粒度（部分账号没权限）

部分代理商账号的"分时数据"权限是默认关的。**联系小红书对接销售开通**，或：
- 备选：用「实时数据」/「时段分析」入口（功能位置因账号略有差异）
- 备选：直接在聚光前端看图表然后截屏

### 情况 2：导出来的 CSV 含合计行

脚本会自动跳过含"合计"字样的行，不用预处理。

### 情况 3：早上 8-10 点数据不全

聚光数据**有滞后**（通常 30-60 分钟），如果你 9:30 跑 8-10 点时报，**9 点段数据可能还没全**。建议：
- 11:00 之后再跑当天 8-10 点
- 当天临时看用「实时数据」（聚光后台自带，无延迟）

---

## 四、命令速查

```bash
# 今天 8-10 点（默认）
python3 juguang_hourly.py --latest --hours 8-10

# 指定文件
python3 juguang_hourly.py ~/Downloads/聚光分时_今天.csv --hours 8-10

# 指定日期（昨天 14-18 点）
python3 juguang_hourly.py --latest --date 2026-05-20 --hours 14-18

# 项目名
python3 juguang_hourly.py --latest --hours 8-10 --project 汤臣倍健

# 不要图（只要文本）
python3 juguang_hourly.py --latest --hours 8-10 --no-viz

# 全天看分布
python3 juguang_hourly.py --latest --hours 0-23
```

---

## 五、时报的 4 张可视化图

每次跑都会自动生成一张 PNG，4 个子图：

1. **分时消耗折线** —— 看哪个小时烧得最多
2. **分时 CTR 柱图** —— 看哪个小时点击效率最高
3. **Feeds vs SEM 分时堆叠** —— 信息流和搜索的小时节奏
4. **Top 5 消耗计划** —— 该时段哪些计划在烧钱

---

## 六、未来：CDP 自动化后

CDP 路线跑通后（见 `README_NOW.md` 第三节），可以做到：

```bash
# 完全自动：从聚光直接拉数据 + 出时报，不用手动导
auto_hourly.sh 8-10
```

预计每天用一次的人工时间从 5 分钟降到 0。
